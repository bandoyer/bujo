# Slice 1.3.2 — hand lettering

A mini slice. The journal gets a hand-lettered voice: **Permanent
Marker becomes the default face for everything** — titles, entry
text, meta, day numbers — with a header cycler to switch hands,
mirroring the theme toggle. The operator picked the faces on the
[Hand-Lettered Titles canvas](https://claude.ai/code/artifact/31d0cb57-850b-4498-95b1-9dab725ccc03):
Permanent Marker (default), Rock Salt, Architects Daughter, Patrick
Hand, Gochi Hand, plus the 1.3 serif look as the sixth option.

**This spec pre-decides the judgment calls; report gaps instead of
inventing rulings.** The mechanics deliberately mirror the theme
system: a cookie, a server-stamped `data-` attribute, token
overrides. No new patterns.

## The lettering values

| Value | Face (display + body + meta) | Cookie |
|---|---|---|
| *(absent)* | Permanent Marker — the default | no cookie |
| `rock-salt` | Rock Salt | stored |
| `architects-daughter` | Architects Daughter | stored |
| `patrick-hand` | Patrick Hand | stored |
| `gochi-hand` | Gochi Hand | stored |
| `serif` | Newsreader + Public Sans + JetBrains Mono (the 1.3 look) | stored |

## Rulings

- **Full treatment.** Each handwriting face applies to display, body,
  and meta text alike via the three font tokens. Every stack keeps
  real fallbacks, and `--font-mono`'s stack keeps JetBrains Mono
  right behind the face so glyphs a handwriting font lacks (`○`, `–`)
  render from the fallback instead of as tofu.
- **Default is the absence of a cookie**, exactly like `system` in
  Theming: choosing "marker" clears the cookie; the five stored
  values live in an allowlist; anything else clears. The layout stamps
  `data-hand="<value>"` on `<html>` only for stored values — bare
  `:root` tokens ARE the marker faces, and each `[data-hand]` block
  redefines the three tokens. (Newsreader/Public Sans move from the
  bare tokens into the `serif` block; the dot-grid/theme colors are
  untouched — lettering and theme are orthogonal axes.)
- **The control is a cycler** in the header utilities, beside the
  theme toggle, same anatomy: a Stimulus-submitted form posting to
  `resource :lettering, only: :update`, cookie `permanent` +
  `same_site: :lax`, redirect back. Labels are mono and short:
  `Hand: marker` → `Hand: rock salt` → `Hand: architects` →
  `Hand: patrick` → `Hand: gochi` → `Hand: serif` → back to marker.
  All six labels are distinct strings (Capybara selects exactly).
- **Generalizing is welcome, duplicating is not.** Theming and
  lettering are the same shape; extracting a shared
  preference-cookie concern (and/or one Stimulus cycler controller
  serving both toggles) is in scope and preferred over a copy —
  jscpd stays clean. Theme behavior must not change: its cookie
  name, values, and tests stay as they are.
- **Fonts stay on Google Fonts** for now: the five new families join
  the existing `<link>` (one request; font binaries download only
  when a face is applied). The `TODO(1.7)` vendoring note now covers
  six families — update its wording, don't resolve it.
- **Hit targets ≥ 44px**, both themes legible in every hand — the
  faces recolor via the existing color tokens automatically.

## File scope

`app/controllers/`, `app/views/`, `app/assets/stylesheets/`,
`app/javascript/`, `config/routes.rb`, `test/`. **No changes** to
`app/models/`, `lib/`, `db/`, `config/application.rb`, or `Gemfile`.
Path-scope baseline: this slice's accepted-spec commit.

## Tests

System lane (browser rules from 1.3 bind):

1. A fresh visit renders with no `data-hand` attribute (marker
   default) and the cycler reads `Hand: marker`.
2. Cycling sets `data-hand` through each stored value in order,
   reload persists, and completing the cycle returns to no attribute.
3. `Hand: serif` restores the 1.3 look's tokens (assert the computed
   font-family of the title changes between marker and serif, the
   flow-8 pattern).
4. Theme and hand compose: dark + a hand both stamp their attributes.

Fast lane: cookie set for each allowlisted value; `serif` stores and
marker clears; a crafted value clears rather than stores or 500s;
auth required on the route; theme tests untouched and green.

## Quality bars

The standard battery; parser mutation stays 1105/1105. Operator
review checks the token discipline (no face defined only inside a
`data-hand` block except as that hand's override), the fallback
stacks, and both toggles composing in a real browser.

## Non-goals (report, don't add)

A settings page; per-screen faces; font-size preferences; vendoring
fonts (1.7); persisting the preference server-side (it is a device
preference, like theme); reordering or renaming the theme toggle.

## Acceptance criteria

The slice is accepted when every item below holds.

- `bin/rails test:system` green, `test/system/daily_log_test.rb`
  covering the four scenarios above.
- `bin/rails test` green, covering the fast-lane list: a cookie stored
  for each of the five allowlisted values, `marker` clearing it, a
  crafted value clearing rather than storing or 500ing, and the route
  redirecting to sign-in when unauthenticated.
- **The theme tests are unmodified.** `git diff` shows no change to
  `test/controllers/themes_controller_test.rb`, and flow 8's body is
  untouched. Theme is the control group for the shared-concern
  extraction: if generalizing changed theme behavior, the extraction is
  wrong, and editing a theme test to match is the one repair that is
  never allowed here.
- `bin/rubocop` clean; fresh `COVERAGE=1 bin/rails test` then
  `crap4rb` ≤ 6; `jscpd --min-tokens 50` clean over `app/ lib/` **with
  no new ignore markers** — the existing `jscpd:ignore-start` over the
  two dark-palette blocks is structural and stands alone. Suppressing a
  Theming/Lettering copy instead of extracting it is the failure this
  slice is meant to avoid. Mutation unchanged at `Bujo::RapidLog*`
  1105/1105.
- The implementation diff, measured from this slice's accepted-spec
  commit, touches only `app/controllers/`, `app/views/`,
  `app/assets/stylesheets/`, `app/javascript/`, `config/routes.rb`, and
  `test/`. `app/models/`, `lib/`, `db/`, `config/application.rb`, and
  `Gemfile` are untouched.

### Ruling: label and value are different strings

This is the one place lettering does *not* mirror theming. A theme's
stored value and its label suffix are the same word, so
`Theme: <%= current_theme %>` works. Hands diverge: the cookie stores
`architects-daughter` while the control reads `Hand: architects`, and
`rock-salt` reads `rock salt`.

The six labels are **exact strings fixed by the spec**, not derived from
the values. Deriving them — `humanize`, `titleize`, swapping hyphens for
spaces — produces "Architects daughter" and silently breaks the browser
tests, which select by exact label. The mapping is explicit: an ordered
value/label pair per hand, with the default's value being the absence of
a cookie and its label `Hand: marker`. The hidden field carries the
**value**; the button shows the **label**.

None of the six collides with another as a substring, and none collides
with the theme toggle's three or with any row action, so exact selection
stays unambiguous — but that is a property to preserve, not a licence to
rename.

### Ruling: the cookie and param are named `hand`

The spec fixes `resource :lettering` for the route and `data-hand` for
the attribute, and does not name the cookie. It is `hand`, as is the
form param — one word for one concept, matching the attribute the layout
stamps and the label the reader sees. "Lettering" names the feature and
its route; "hand" names the value the reader picks and the app stores.
A later reader tempted to "fix" the mismatch should not: the route is the
spec's, and renaming it is out of scope.

Theme's cookie stays `theme`. If the shared concern parameterizes the
cookie name, both names are supplied by their callers rather than derived
from a class name, so neither can drift when the other changes.

### Ruling: labels are server-rendered, so the cycler stays thin

The existing theme cycler never rewrites the button's text — it sets the
hidden value, optimistically stamps the attribute on `<html>` to avoid a
flash, and submits; the new label arrives with the server's re-render.
That is what makes one controller serve both toggles: it needs only the
ordered list of values and the name of the attribute to stamp, and never
needs to know a label at all.

Keep the optimistic stamp when generalizing. It is the reason the theme
does not flash between click and reload, and dropping it would be a
regression no assertion in flow 8 would catch.

### Ruling: no test may depend on a font binary downloading

Faces load from Google Fonts, which a test run cannot be assumed to
reach. `getComputedStyle(element).fontFamily` returns the *specified*
stack, not the face that actually rendered, so scenario 3 discriminates
marker from serif whether or not a single byte of font downloaded — the
same reason flow 8 reads `backgroundColor` rather than a screenshot.
Assert on the stack. Never assert on rendered glyph metrics, text width,
or a screenshot, and never add a network wait for fonts.

### Note: the vendoring TODO covers eight families

Six hands, but eight font families: five handwriting faces plus the
three the `serif` hand restores. The `TODO(1.7)` comment is reworded to
say what is true; a comment that miscounts what it defers is worse than
no comment.
