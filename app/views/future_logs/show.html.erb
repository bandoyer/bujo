<% content_for :title, "Future Log · Bujo" %>

<main class="daily-log future-log">
  <header class="daily-log__header future-log__header">
    <div class="daily-log__heading">
      <p class="daily-log__eyebrow future-log__eyebrow">FROM <%= (@today + 1.day).strftime("%b %-d · %Y").upcase %></p>
      <h1>Future Log</h1>
    </div>
  </header>

  <section class="future-log__runway" aria-label="Upcoming entries">
    <% @months.each do |month| %>
      <% entries = @entries_by_month.fetch(month, []) %>
      <section class="future-log__month <%= "future-log__month--empty" if entries.empty? %>"
               data-month="<%= month.strftime("%Y-%m") %>">
        <h2><%= month.strftime("%B %Y").upcase %></h2>
        <% entries.each do |entry| %>
          <%= link_to daily_log_path(date: entry.occurs_on.iso8601),
                      id: "future_entry_#{entry.id}",
                      class: "future-entry" do %>
            <span class="future-entry__day"><%= entry.occurs_on.day %></span>
            <%= render "entries/entry_body", entry: entry %>
          <% end %>
        <% end %>
      </section>
    <% end %>
  </section>

  <%= render "shared/tab_bar", active: :future %>
</main>
