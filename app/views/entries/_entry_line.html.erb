<%# The journal line is shared by interactive task toggles and static rows. %>
<span class="entry__signifier" aria-label="<%= entry.priority? ? "Priority" : "Not priority" %>"><%= "*" if entry.priority? %></span>
<%= render "entries/entry_body", entry: entry %>
